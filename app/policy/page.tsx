//STEP: 163 
"use client";
import React, { useState } from 'react';
import Heading from '../utils/Heading';
import Header from '../components/Header';
import Footer from '../components/Footer/Footer';
import Policy from './Policy';
//defining-props
type Props = {}
//creating-data
const Page = (props: Props) => {
  const [open, setOpen] = useState(false);
  const [activeItem, setActiveItem] = useState(3);
  const [route, setRoute] = useState("Login");
  return (
    <div className="max-w-[1920px]">
        <Heading title='Policy -Elearning' description='Elearning is a learning management system for helping programmers.' keywords='programming, mern' />
        <Header open={open} setOpen={setOpen} activeItem={activeItem} route={route} setRoute={setRoute} />
        {/* now, create a file named "./Policy.tsx", code in it and then come back here. */}
        <Policy />
        <Footer />
    </div>
  )
}
//exporting-data
export default Page;
//OVER: 163("c": ../faq & ../faq/page.tsx and "m": ../faq/page.tsx) 