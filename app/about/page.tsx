//STEP: 162 
"use client";
import React, { useState } from 'react';
import Heading from '../utils/Heading';
import Header from '../components/Header';
import About from './About';
import Footer from '../components/Footer/Footer';
//defining-props
type Props = {}
//creating-data
const Page = (props: Props) => {
  const [open, setOpen] = useState(false);
  const [activeItem, setActiveItem] = useState(2);
  const [route, setRoute] = useState("Login");
  return (
    <div className="max-w-[1920px]">
        <Heading title='About us -Elearning' description='Elearning is a learning management system for helping programmers.' keywords='programming, mern' />
        <Header open={open} setOpen={setOpen} activeItem={activeItem} route={route} setRoute={setRoute} />
        {/* now, create a file named "./About.tsx", code in it and then come back here. */}
        <About />
        <Footer />
    </div>
  )
}
//exporting-data
export default Page;
//OVER: 162("c": ../policy & ../policy/page.tsx and "m": ../policy/page.tsx) 