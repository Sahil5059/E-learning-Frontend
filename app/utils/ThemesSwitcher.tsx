//STEP: 8 
//now, open the "client" folder terminal and type: "npm i react-icons"
'use client';
import { useState,useEffect } from "react";
import { useTheme } from "next-themes";
import { BiMoon,BiSun } from "react-icons/bi";
//creating page
export const ThemeSwitcher = () => {
    const [mounted, setMounted] = useState(false);
    const {theme, setTheme} = useTheme();
    useEffect(() => setMounted(true), []);
    if(!mounted){return null}; //As per my understanding, useEffect generally takes a little time to load, so, till it loads completely, this line will start execting itself and then the useEffect() will completely. Hence, you get the "sudden-appearance" effect of the dark-light mode button when you reaload the page.
    return(
        <div className="flex items-center justify-center mx-4">
            {
                theme === "light" ? (<BiMoon className="cursor-pointer" fill="black" size={25} onClick={() => setTheme("dark")} />) : (<BiSun size={25} className="cursor-pointer text-white" onClick={() => setTheme("light")} />)
            }
        </div>
    )
}
//OVER: 8("m": ../components/Header.tsx) 